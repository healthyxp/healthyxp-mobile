import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 20,
    gap: 20
  },
  body: {
    flexDirection: 'row',
    gap: 10

  },

  logo: {
    width: 40,
    height: 40
  },

  linkCriarConta: {
    fontWeight: 'bold'
  }
});
